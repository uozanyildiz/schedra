{
  "name": "schedra-workspace",
  "private": true,
  "type": "module",
  "license": "MIT",
  "packageManager": "pnpm@10.15.1",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm --filter @schedra/core build && pnpm --filter @schedra/react build && pnpm --filter @schedra/react-popover build && pnpm -r typecheck",
    "lint": "eslint .",
    "dev": "pnpm --filter @schedra/demo dev",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "release:check": "pnpm typecheck && pnpm test && pnpm build && pnpm lint && pnpm format:check && pnpm --filter schedra publish --dry-run --access public --no-git-checks",
    "release:publish": "pnpm --filter schedra publish --access public"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^24.0.0",
    "eslint": "^10.7.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^17.7.0",
    "prettier": "^3.6.2",
    "rimraf": "^6.1.3",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.64.0",
    "vitest": "^3.2.4"
  }
}
